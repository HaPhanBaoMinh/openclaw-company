apiVersion: v1
kind: Service
metadata:
  name: openclaw-svc
  namespace: openclaw
  labels:
    app: openclaw
spec:
  type: ClusterIP
  selector:
    app: openclaw
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
